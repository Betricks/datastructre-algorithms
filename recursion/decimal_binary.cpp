#include <bits/stdc++.h>
#include <cmath>

using namespace std;


string decimalToBinary(int decimal){
    
    int result = floor(decimal / 2);
    int binaries = decimal % 2;
    
    string Mybinary;
    // Mybinary = Mybinary +  string binaries;
    
    
    if(decimal == 0){
        return Mybinary;
    }
    
    
    
    
}